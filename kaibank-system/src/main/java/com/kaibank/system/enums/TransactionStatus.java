package com.kaibank.system.enums;

/**
 * The TransactionStatus enum contains possible transaction status.
 * @author Pabasara
 * @version 1.0
 * @since 1.0
 */
public enum TransactionStatus {
  SUCCESS,
  FAILED,
  PENDING,
  HOLD,
  CANCELLED
}
